---
import { MystoviaLogo } from "../assets/logos";
import CartButton from "./CartButton";
import HeaderAuth from "./HeaderAuth";
import HeaderNav from "./HeaderNav";
import MobileNav from "./MobileNav";
---

<header class="fixed z-100 top-0 flex items-center justify-between w-full px-4 md:px-8 lg:px-16 py-2 backdrop-blur-md bg-black/30">
  <!-- Logo -->
  <a href="/" class="flex-shrink-0">
    <img
      src={MystoviaLogo.src}
      alt="Mystovia Logo"
      class="w-32 md:w-40 lg:w-50 h-auto cursor-pointer transform transition-all hover:mb-2"
    />
  </a>

  <!-- Desktop Navigation -->
  <HeaderNav client:load />

  <!-- Desktop Auth/Cart -->
  <aside class="hidden md:flex items-center gap-2 lg:gap-4 flex-shrink-0">
    <CartButton client:only="react" />
    <HeaderAuth client:only="react" />
  </aside>

  <!-- Mobile Menu -->
  <MobileNav client:load />
</header>
