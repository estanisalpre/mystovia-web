---
import { MystoviaLogo } from "../assets/logos";
import CartButton from "./CartButton";
import HeaderAuth from "./HeaderAuth";
import HeaderNav from "./HeaderNav";
import MobileNav from "./MobileNav";
import DiamondPattern from "./ui/DiamondPattern.astro";
import GoldenLine from "./ui/GoldenLine.astro";
---

<header class="fixed z-900 top-0 flex items-center justify-between w-full px-4 md:px-8 lg:px-16 py-2 bg-gradient-to-b from-gray-900/95 via-gray-900/90 to-gray-900/80 backdrop-blur-md border-b border-yellow-600/20">
  <!-- Decorative bottom border glow -->
  <GoldenLine position="bottom" thickness="thin" opacity={0.4} />

  <!-- Subtle pattern overlay -->
  <DiamondPattern opacity={0.03} size={20} />

  <!-- Logo -->
  <a href="/" class="flex-shrink-0 relative z-10">
    <img
      src={MystoviaLogo.src}
      alt="Mystovia Logo"
      class="w-32 md:w-40 lg:w-50 h-auto cursor-pointer transform transition-all hover:mb-2"
    />
  </a>

  <!-- Desktop Navigation -->
  <HeaderNav client:load />

  <!-- Desktop Auth/Cart -->
  <aside class="hidden lg:flex items-center gap-2 lg:gap-4 flex-shrink-0 relative z-10">
    <CartButton client:only="react" />
    <HeaderAuth client:only="react" />
  </aside>

  <!-- Mobile Cart + Menu -->
  <div class="flex lg:hidden items-center gap-2 flex-shrink-0 relative z-10">
    <CartButton client:only="react" />
    <MobileNav client:load />
  </div>
</header>
